export async function findOperationalDetail(db, workOrderId) {
  const { rows } = await db.query(`SELECT wo.id, wo.wo_number AS "woNumber", wo.sales_order_id AS "salesOrderId", so.so_number AS "salesOrderNumber", wo.sales_order_item_id AS "salesOrderItemId", wo.status, wo.started_at AS "startedAt", wo.completed_at AS "completedAt", wo.rts_at AS "rtsAt", wo.created_at AS "createdAt", wo.updated_at AS "updatedAt", wos.id AS "snapshotId", wos.customer_name AS "customerName", wos.product_name AS "productName", wos.quantity, wos.material, wos.specification, wos.dimension, wos.color, wos.thickness, wos.production_notes AS "productionNotes", wos.artwork_file_url AS "artworkFileUrl", wos.artwork_drive_url AS "artworkDriveUrl" FROM work_orders wo JOIN sales_orders so ON so.id = wo.sales_order_id LEFT JOIN work_order_snapshots wos ON wos.work_order_id = wo.id WHERE wo.id = $1`, [workOrderId]);
  if (!rows[0]) return null;
  const { rows: events } = await db.query(`SELECT id, process_type AS "processType", status, started_at AS "startedAt", completed_at AS "completedAt", notes, created_by AS "createdBy", created_at AS "createdAt" FROM production_events WHERE work_order_id = $1 ORDER BY created_at ASC`, [workOrderId]);
  return { ...rows[0], productionEvents: events };
}
