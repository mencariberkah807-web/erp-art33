{
  "name": "erp-art33-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-db.mjs && node --watch src/server.js",
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.mjs",
    "build": "npm run migrate",
    "test": "node --test",
    "create-admin": "node scripts/create-admin.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "nodemailer": "^7.0.6",
    "pg": "^8.16.3"
  }
}
